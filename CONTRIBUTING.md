# المساهمة في FUGU-MAX

شكرًا لاهتمامك. المشروع بحثي بالدرجة الأولى، ومعيار القبول فيه **القياس** لا الإعجاب.

## قبل أن تبدأ

1. افتح Issue لوصف الفكرة أو الخطأ.
2. لأي تعديل على الاستراتيجية أو الميزات أو الأوزان: اذكر **الفرضية** و**كيف تُختبر**.

## قاعدة القبول الذهبية

> أي إضافة يجب أن تُثبت أثرها على **نفس البيانات**، **نفس التكاليف**، و**نفس قواعد التنفيذ**.

قارن دائمًا عبر `baselines.py` (A/B/C/D/E)، وأرفق جدولًا قبل/بعد يحتوي على:
`total_return`, `sharpe`, `max_drawdown`, `n_trades`، وعلى بيانات خارج العينة إن أمكن.

## ما يُرفض

- إضافة مؤشر "لأنه مشهور" أو "يبدو متقدّمًا" بلا دليل قياسي.
- أي كود يستخدم معلومة من المستقبل (look-ahead) — يُرفض فورًا.
- أي محاولة لإزالة أو تخفيف سياسة **SL-أولًا عند الالتباس**.
- أي التزام بمفاتيح أو توكنات داخل المستودع.

## خطوات العمل

```bash
git checkout -b feature/وصف-مختصر
# ... عدّل ...
bash scripts/check.sh          # تجميع + اختبارات
git commit -m "feat: وصف مختصر"
git push origin feature/وصف-مختصر
```

## نمط الـcommit

`feat:` ميزة · `fix:` إصلاح · `docs:` توثيق · `test:` اختبار · `refactor:` إعادة هيكلة · `perf:` أداء

## الاختبارات

- كل منطق جديد يحتاج اختبار وحدة في `tests/`.
- أي تعديل يمسّ التوقيت يجب أن يمرّ في `tests/test_features.py` و`tests/test_labels.py` و`tests/test_backtest.py`.
- `tests/test_backtest.py::test_frozen_engine_does_not_learn` يجب أن يبقى ناجحًا دائمًا.

## النصوص واللغة

الوثائق والتعليقات بالعربية، وأسماء المتغيّرات والدوال بالإنجليزية.

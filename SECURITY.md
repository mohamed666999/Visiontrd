# سياسة الأمان

## الإبلاغ عن ثغرة

لا تفتح Issue عامًّا لثغرة أمنية. راسل المشرفين مباشرة عبر بريد المستودع أو
عبر خاصية **Report a vulnerability** في تبويب Security على GitHub.

سنؤكد الاستلام خلال 72 ساعة، وننشر تصحيحًا في أقرب إصدار.

## قواعد التعامل مع الأسرار

- **لا** تُضِف أبدًا ملف `.env` أو أي مفتاح API أو توكن بوت إلى المستودع.
- المفاتيح تُقرأ في `config.py` من البيئة أو من `.env` المحلي فقط.
- ملف `.env` مستثنى في `.gitignore`، ورغم ذلك راجع `git status` قبل كل commit.
- التوكنات والمفاتيح لا تُطبع في السجلات ولا تُخزَّن في قاعدة بيانات الـObserver.

## إن انكشف سرّ

1. **بينانس**: ألغِ المفتاح فورًا من لوحة API Management وأنشئ مفتاحًا جديدًا،
   وقيّد المفتاح الجديد بصلاحيات وبعنوان IP إن أمكن.
2. **Telegram**: أرسل `/revoke` إلى @BotFather لتوليد توكن جديد؛ التوكن القديم يُبطَل.
3. راجع سجل git — إن كان السر قد أُدرج في commit سابق، فهو محفوظ في التاريخ حتى لو
   حُذف من آخر نسخة. نظّف التاريخ (مثلًا `git filter-repo`) أو اعتبر السر منكشفًا دائمًا.

## نطاق العمل الآمن

- الوضع الافتراضي هو `demo`. لا تُشغّل `live` قبل اختبار طويل على الديمو.
- الحساب الحقيقي معطّل تقنيًا حتى تُضبط `FUGU_MODE=live` و`LIVE_CONFIRM=true` صراحةً.
- هذا المشروع للبحث والتعلّم، وليس توجيهًا استثماريًا.
